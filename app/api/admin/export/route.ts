import { getReplies } from "@/lib/repository";
import { isAdmin } from "@/lib/security";
const safe=(value:string)=>'"'+value.replaceAll('"','""').replace(/^[=+\-@]/,"'$&")+'"';
export async function GET(){if(!(await isAdmin()))return new Response("Unauthorized",{status:401});const replies=await getReplies();const rows=[["Name","Email","Phone","Reply","Approval","Party size","Guests","Note","Last updated"],...replies.map((r)=>[r.fullName,r.email,r.phone,r.status==="celebrating"?"Can't wait to celebrate":"Celebrating from afar",r.status==="celebrating"?r.approval:"",String(r.guestCount),r.guestNames.join("; "),r.note,r.updatedAt])];const csv=rows.map((row)=>row.map(safe).join(",")).join("\r\n");return new Response(csv,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":"attachment; filename=save-the-date-replies.csv"}})}
